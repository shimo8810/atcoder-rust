use proconio::{fastout, input};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
      N: usize,
      X: u64,
      Y: u64,
      A: [u64; N],
      B: [u64; N]
    }

    let mut ans = 0;

    for b in B.iter() {
        for a in A.iter() {
            //
        }
    }
    println!("{}", ans);
}
