use proconio::{fastout, input};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
        N: usize,
        M: u32,
        A: [u32; N]
    }

    let mut ans = 0;
    println!("{}", ans);
}
