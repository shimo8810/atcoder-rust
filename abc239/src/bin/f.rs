use proconio::{fastout, input};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
        N: usize,
        M: usize,
        D: usize
    }

    let mut ans = 0;
    println!("{}", ans);
}
