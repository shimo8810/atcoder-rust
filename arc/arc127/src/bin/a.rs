use proconio::{fastout, input};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
      N: usize,
    }
    let s = N.to_string().len();

    let mut ans = 0;
    for i in 1..=s {
        for j in 1..=i {

        }
    }
    println!("{}", ans);
}
