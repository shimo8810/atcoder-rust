[package]
name = "arc130"
version = "0.1.0"
edition = "2018"

# dependencies added to new project
[dependencies]
itertools = "0.10.1"
num = "0.4.0"
proconio = { version = "0.4.3", features = ["derive"] }

[profile.release]
lto = true
panic = 'abort'
