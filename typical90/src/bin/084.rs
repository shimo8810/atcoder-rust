use proconio::{fastout, input, marker::Chars};

#[allow(non_snake_case)]
#[fastout]
fn main() {
    input! {
      N: usize,
      S: Chars
    }

    let mut ans = 0;
    let mut h = 0;
    let mut t = 0;

    loop {
        
    }

    println!("{}", ans);
}
